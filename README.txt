#my-project

This project is a object repository of a Web project.

com.mypoject.qa.testing.test.flows
Above package will have a classes which represents short flows which will extend the BaseWebDriver.

com.myproject.qa.testing.pages
Above package will have classes which has all possible actions and verifications.

com.myproject.qa.testing.objects
Above package will be having enumeration classes which will have all locator information page by page.